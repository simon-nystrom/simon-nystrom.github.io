const PrivacyPolicyPage = () => {
  return (
    <div>
      This page uses Google Analytics (and its required cookies) to distinguish
      unique users and sessions for a given user.
    </div>
  );
};

export default PrivacyPolicyPage;
